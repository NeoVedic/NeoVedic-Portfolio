# Email Configuration (for development)
# Set these in Replit Secrets for production
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=
SMTP_PASSWORD=
SMTP_SENDER_NAME=Website Form

# Receiver email for contact and career forms
CONTACT_RECEIVER_EMAIL=
